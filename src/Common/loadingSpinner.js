import React from 'react';

const LoadingSpinner = () => (
	<div>
	<i style={{fontSize:'50px', color: 'grey'}} className="fa fa-spinner fa-spin" /> Loading...
	</div>
);

export default LoadingSpinner;
